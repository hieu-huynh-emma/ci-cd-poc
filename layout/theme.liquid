<!doctype html>
<html class="no-js" lang="{{ request.locale.iso_code }}">
<head>
  {% render 'critical-resources', isOptimized: false %}

  {% render 'hso-no-follow-no-index' %}
  {% render 'page-metadata' %}
  {% render 'hreflang-tags' %}
  {% render 'vendors', isOptimized: false %}
  {% render 'page-scripts', isOptimized: false %}
  {% render 'page-styles', isOptimized: false %}

  {{ content_for_header }}
</head>

{% render 'shopify-theme-settings', isOptimized: false %}

<body>

  <div class="sticky top-0 z-[101]">
    {% section 'announcement-bar' %}
    {% section 'header' %}
  </div>

  <main id="MainContent" role="main">
    {{ content_for_layout }}
  </main>

  {% section 'footer' %}

  <ul hidden>
    <li id="a11y-refresh-page-message">{{ 'general.accessibility.refresh_page' | t }}</li>
    <li id="a11y-selection-message">{{ 'general.accessibility.selection_help' | t }}</li>
    <li id="a11y-slideshow-info">{{ 'home_page.slideshow.navigation_instructions' | t }}</li>
  </ul>

  {% render 'cart-drawer' %}
  {% render 'affirm-popup' %}
  {% render 'store-localization' %}
  {% render 'freshchat' %}
  {% render 'cookie-consent-bar' %}
</body>
</html>
