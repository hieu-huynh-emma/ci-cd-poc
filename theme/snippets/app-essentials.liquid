
<script defer src="{{ 'jquery-3.6.0.min.js' | file_url }}"></script>

<link rel="preconnect" href="https://emma-sleep.ca/cdn/shop/files"/>

<script defer>
    const Shop = {
        id: "{{ shop.id | json }}",
        assetUrl: "{{ "a" | asset_url }}".match(/^.*\//g)[0],
        fileUrl: "{{ "a" | file_url }}".match(/^.*\//g)[0],
        fileImgUrl: "{{ "a" | file_img_url }}".match(/^.*\//g)[0],
        settings: {
            saleTimer: "{{ settings.sale_timer }}"
        },
        addonServiceIds: "{{ shop.metafields.globals.addon_services }}"
    }
</script>

<script defer src="{{ 'helpers.js' | asset_url }}"></script>
<script defer src="{{ 'custom-elements.js' | asset_url }}"></script>
<script defer src="{{ 'web-components.js' | asset_url }}"></script>
<script defer src="{{ 'resource-coordinator.js' | asset_url }}"></script>
<script defer src="{{ 'cookie-parser.js' | asset_url }}"></script>
<script defer src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>

{% render 'critical-stylesheet' %}

{% render 'weglot_hreftags' %}
{% render 'weglot_switcher' %}
