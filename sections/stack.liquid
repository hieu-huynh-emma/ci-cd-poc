{% liquid
    assign settings = section.settings
%}

<container-block
    type="stack"
    :settings="{% content_for "block", type: "container", id: "container" %}"
>
    <frame-block :settings="{% content_for "block", type: "frame", id: "frame" %}">
        {% content_for 'blocks' %}
    </frame-block>
</container-block>


{% schema %}
{
  "name": "Stack",
  "tag": "section",
  "blocks": [
    {
      "type": "heading"
    },
    {
      "type": "paragraph"
    },
    {
      "type": "button"
    },
    {
      "type": "container"
    },
    {
      "type": "frame"
    }
  ],
  "settings": [
  ],
  "presets": [
    {
      "name": "Stack",
      "blocks": [
        {
          "type": "container",
          "id": "container",
          "static": true,
          "name": "Container",
          "settings": {
          },
          "blocks": {}
        },
        {
          "type": "frame",
          "id": "frame",
          "static": true,
          "name": "Frame",
          "settings": {
            "bgcolor": "#00000000",
            "spacing": "base",
            "margin_y": 0,
            "margin_x": 0,
            "padding_y": 10,
            "padding_x": 0,

            "border_radius": 0,
            "boundless": false,
            "content_width": 100
          },
          "blocks": {}
        }
      ]
    }
  ]
}
{% endschema %}
