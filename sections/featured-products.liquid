{% assign productBlocks = section.blocks | where: "type", "product-block" %}

<section id="featured-products-section">
  <div class="wrapper">
    <div class="headings">
      <h2 class="heading-2 text-center text-charade">{{ section.settings.title | remove: "<p>" | remove: "</p>" }}</h2>
      <p class="subheading text-comet">{{ section.settings.subtitle }}</p>
    </div>

    <div class="customer-reviews">
      {%- for block in productBlocks -%}
        {% assign product = block.settings.product %}

        <div class="card">
          <div class="top-section">
            <p class="card__badge">
              {{ block.settings.badge }}
            </p>
            <div class="w-full">
              <img
                src="{{ block.settings.image | img_url: 'grande' }}" width="100%" height="auto"
                class="h-full" loading="lazy"
              />
            </div>

            <h3 class="text-[24px] text-charade font-normal text-center self-center">{{ product.title }}</h3>
            <p class="text-charade mb-2">{{ block.settings.desc | remove: "<p>" | remove: "</p>" }}</p>
            <div class="w-full">
              <div class="mattress-comparison">
                <div class="mattress-comparison__item">
                  <p>Comfort</p>
                  <div class="mattress-comparison__bar mattress-comparison__bar--outer">
                    <div class="mattress-comparison__bar mattress-comparison__bar--inner fill-{{ block.settings.comfort_meter }}"></div>
                  </div>
                </div>

                <div class="mattress-comparison__item">
                  <p>Cooling</p>
                  <div class="mattress-comparison__bar mattress-comparison__bar--outer">
                    <div class="mattress-comparison__bar mattress-comparison__bar--inner fill-{{ block.settings.cooling_meter }}"></div>
                  </div>
                </div>

                <div class="mattress-comparison__item">
                  <p>Motion Isolation</p>
                  <div class="mattress-comparison__bar mattress-comparison__bar--outer">
                    <div class="mattress-comparison__bar mattress-comparison__bar--inner fill-{{ block.settings.motion_isolation_meter }}"></div>
                  </div>
                </div>
              </div>
            </div>
          </div>


          <div class="footer-section">
            <div class="starting-at">
              <span class="text-[14px] font-normal">starting at</span>
              <div class="flex items-center">
                {% if product.compare_at_price > product.price %}
                  <p class="font-semibold mr-1 font-helvetica">{{ product.price | money_without_trailing_zeros }}</p>
                  <p class="line-through font-helvetica">{{ product.compare_at_price | money_without_trailing_zeros }}</p>
                {% else %}
                  <p class="font-semibold font-helvetica">{{ product.price | money_without_trailing_zeros }}</p>
                {% endif %}
              </div>
            </div>

            <a class="btn" href="{{ product.url }}" rel="noreferrer">
              Shop Now
            </a>
          </div>


        </div>
      {%- endfor -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Featured Products",
  "blocks": [
    {
      "type": "product-block",
      "name": "Product Block",
      "settings": [
        {
          "id": "product",
          "type": "product",
          "label": "Block Product"
        },
        {
          "id": "badge",
          "type": "text",
          "label": "Block Badge",
          "default": "Best Value"
        },
        {
          "id": "image",
          "type": "image_picker",
          "label": "Block Image"
        },
        {
          "id": "desc",
          "type": "richtext",
          "label": "Block Description"
        },
        {
          "type": "range",
          "id": "comfort_meter",
          "min": 1,
          "max": 100,
          "step": 1,
          "label": "Comfort Meter",
          "default": 80
        },
        {
          "type": "range",
          "id": "cooling_meter",
          "min": 1,
          "max": 100,
          "step": 1,
          "label": "Cooling Meter",
          "default": 80
        },
        {
          "type": "range",
          "id": "motion_isolation_meter",
          "min": 1,
          "max": 100,
          "step": 1,
          "label": "Motion Isolation Meter",
          "default": 80
        }
      ]
    }
  ],
  "settings": [
    {
      "id": "title",
      "type": "richtext",
      "label": "Title",
      "default": "<p>Hit snooze! Sleep is about to get <i>wayyy better.</i></p>"
    },
    {
      "id": "subtitle",
      "type": "text",
      "label": "Subtitle",
      "default": "Our sleep experts have mastered designing award-winning mattresses crafted to keep you relaxed and recharged, no matter what kind of sleeper you are. Climb on your best sleep yet!"
    }
  ]
}
{% endschema %}
