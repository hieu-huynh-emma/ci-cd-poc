<!doctype html>
<html class="no-js" lang="{{ request.locale.iso_code }}">


<head>
    {% render 'pagefly-head' %}
    {% render 'critical-resources' %}
    {% render 'page-metadata' %}

    {% render 'hreflang-tags' %}

    {{ content_for_header }}

    {% render 'shopify-theme-settings' %}

    {% render 'page-styles' %}
    {% render 'layout-styles' %}

    <script>document.documentElement.className = document.documentElement.className.replace("no-js", "js");</script>
    {% render 'delivery_coder' %} {% include 'kart_discount' %}

    <!-- Extend - Render extend-configuration -->
    {% render 'extend-configuration' %}
    <!-- Extend - End code -->
    {% render 'weglot_hreftags' %}
    {% render 'weglot_switcher' %}
    <script async type="text/javascript" src="https://try.abtasty.com/b98579b87d8565047076037f703e1727.js"></script>

</head>

<body class="gradient">

{% section 'splash-screen' %}

<a class="skip-to-content-link button visually-hidden" href="#MainContent">
    {{ "accessibility.skip_to_text" | t }}
</a>

{% sections 'header-group' %}
{% sections 'affiliate-group' %}

<main id="MainContent" class="content-for-layout focus-none" role="main" tabindex="-1">
    {{ content_for_layout }}
</main>

{% sections 'footer-group' %}

<ul hidden>
    <li id="a11y-refresh-page-message">{{ 'accessibility.refresh_page' | t }}</li>
</ul>


<script>
    window.routes = {
        cart_add_url: '{{ routes.cart_add_url }}',
        cart_change_url: '{{ routes.cart_change_url }}',
        cart_update_url: '{{ routes.cart_update_url }}',
        cart_clear_url: '{{ routes.cart_clear_url }}',
        predictive_search_url: '{{ routes.predictive_search_url }}',
    };

    window.cartStrings = {
        error: `{{ 'sections.cart.cart_error' | t }}`,
        quantityError: `{{ 'sections.cart.cart_quantity_error_html' | t }}`,
    };

    window.variantStrings = {
        addToCart: `{{ 'products.product.add_to_cart' | t }}`,
        soldOut: `{{ 'products.product.sold_out' | t }}`,
        unavailable: `{{ 'products.product.unavailable' | t }}`,
    };

    window.accessibilityStrings = {
        shareSuccess: `{{ 'general.share.success_message' | t }}`,
    };
</script>

{%- if settings.predictive_search_enabled -%}
    <script src="{{ 'predictive-search.js' | asset_url }}" defer="defer"></script>
{%- endif -%}

{% render 'vendors' %}
{% render 'page-scripts' %}

{% render 'widgets' %}


</body>
</html>
