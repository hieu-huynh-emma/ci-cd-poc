<style>
    body {
        margin: 0
    }

    script[type="quickscript"], link[rel="quickscript"] {
        display: block;
        width: 1px;
        height: 1px
    }

    .splide:not(.is-initialized)  {
        display: none !important;
    }

    .visually-hidden {
        position: absolute !important;
        overflow: hidden;
        clip: rect(0 0 0 0);
        height: 1px;
        width: 1px;
        margin: -1px;
        padding: 0;
        border: 0
    }

    h1, h2, h3, h4, h5, h6, p {
        margin: 0;
        opacity: 0
    }

    lazy-component {
        position: relative;
    }

    loading-overlay {
        position: absolute;
        inset: 0;
        z-index: 1;
    }

    img {
        height: auto
    }

  .shopify-challenge__container {
    display: grid;
    gap: 16px;
    align-items: center;
    align-content: center;
    min-height: 50vh;
  }

  .shopify-challenge__container form {
       display: grid;
       gap: 16px;
   }
</style>
